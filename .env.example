# Copy to server/.env for local development.
# See server/.env.example for all variables (Supabase DATABASE_URL, DIRECT_URL, Stripe, SendGrid, etc.).

# Marketing landing page GA4 (optional; only fires on betterblog.xyz / www.betterblog.xyz in production)
# VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
