name: E2E

on:
  pull_request:
    paths:
      - "client/src/app/pages/Configure.tsx"
      - "client/src/app/components/BlogPreviewIframe.tsx"
      - "scripts/renderer.js"
      - "e2e/**"
      - "playwright.config.ts"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/e2e.yml"
  push:
    branches: [main]
    paths:
      - "client/src/app/pages/Configure.tsx"
      - "client/src/app/components/BlogPreviewIframe.tsx"
      - "scripts/renderer.js"
      - "e2e/**"
      - "playwright.config.ts"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/e2e.yml"

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run E2E suite
        run: npx playwright test --project=chromium
        env:
          CI: "true"

      - name: Upload Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          if-no-files-found: ignore

  renderer-layout:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Run renderer layout suite
        run: npm run test:e2e:renderer
        env:
          CI: "true"

      - name: Upload renderer Playwright report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report-renderer
          path: playwright-report/
          if-no-files-found: ignore
